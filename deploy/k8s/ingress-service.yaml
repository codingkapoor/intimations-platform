apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-service
  namespace: intimations
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: hello-cluster-ip-service
              servicePort: 80
